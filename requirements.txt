fastapi>=0.115
uvicorn[standard]>=0.32
sqlmodel>=0.0.22
jinja2>=3.1
python-multipart>=0.0.20
python-dotenv>=1.0
apscheduler>=3.10,<4
httpx>=0.27
# Reads the machine's timezone locally, so weather and the scheduler follow a
# move without an .env edit. Already pulled in by apscheduler; pinned here
# because config.py imports it directly.
tzlocal>=5.2

# Portfolio tier only. Everything else runs on the local model.
anthropic>=0.121.0
# Local model: talks to llama.cpp's OpenAI-compatible endpoint
openai>=1.60

# Google: Gmail + Calendar, desktop OAuth with a cached token
google-api-python-client>=2.150
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# dev
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.8
